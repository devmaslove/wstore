name: reactive_store
description: A state management library that helps add reactivity to widgets.
version: 0.0.5
repository: https://github.com/devmaslove/reactive_store

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
